# V7.10.0 验证报告

本报告记录代码候选 e54158fd88da4a64000da78e9e40846893b58b7a 在 2026-09-15 的验证。发行、来源证明、公开下载和 Pages 的实际状态由对应目标提交的工作流及发布读回记录分别证明。

本地完整包验证通过：480 项包测试、231 项 runtime 测试。状态/诊断 41 项、基准工具 8 项、恢复及资源 26 项、交付状态合同 76 项定向验证通过。Windows 的链接权限跳过项由 Ubuntu CI 补验；Windows/Ubuntu × Python 3.11/3.13 的四组完整 CI 及双语构建均通过。[候选 CI](https://github.com/JimmyVGDY/codex-long-term-assistant-skills/actions/runs/34932132143)、[11 版本兼容矩阵](https://github.com/JimmyVGDY/codex-long-term-assistant-skills/actions/runs/34932132285)。

关键反例覆盖：正常基础安装、停用/未知/重复注册、控制准备条件失效、事务恢复优先级、读取错误和 journal 变化、过大/损坏记录、身份冲突、脏文件内容变化、外部转换器不执行、查询字节不变性及子进程回收。完整 STALE 查询在两条入口均返回 0，仍明确提示证据过期；部分/未知为 1，参数或身份错误为 2。实际 POSIX 启动器也验证了显式无效解释器、有效解释器和无需 Python 的帮助路径。

双语来源/投影、严格本地化、内部链接及严格 MkDocs 构建通过。独立逻辑只读复审和限定返修核对无剩余代码阻塞项；安装、注册、新进程加载与业务生效的证据范围保持分离。

真实任务对照使用公开 fixture、相同批准档位，每场景每版三个样本。只统计成功样本的耗时/工具数中位数；未完成样本保留：

| 场景 | 旧版成功 | 候选成功 | 旧版/候选耗时中位数（秒） | 旧版/候选工具数中位数 |
|---|---:|---:|---:|---:|
| 简单修复 | 2/3 | 3/3 | 108.9 / 163.0 | 8 / 14 |
| 普通功能 | 2/3 | 3/3 | 186.7 / 116.1 | 15.5 / 15 |
| 只读任务恢复 | 2/3 | 3/3 | 149.3 / 148.5 | 16 / 16 |

样本仅提供描述性结果，不能据此声称普遍提速或统一降低工具成本。辅助读取字符和自然语言澄清无法可靠取得，记为 UNKNOWN。初版恢复采样多加了代码修复目标，已按原计划的只读目标重采；原始扩展样本保留但不进入上表。简单/功能样本来自 1174e2d，后续改动限 POSIX、恢复退出码和报告，未改变这两个 Windows 场景路径；恢复样本来自 e54158f。

确定性流程各版每项 20 个样本：status、doctor、恢复等价流程全部成功。恢复从三个显式 CLI 调用减少为一个，观测中位耗时由约 1.60 秒增至 2.39 秒；新流程包含有界采集和稳定性核验，不能把调用数减少等同于总耗时降低。同一修复候选的 status/doctor/resume 各 20 对交替包装层测量，median/p95 增量均在 max(200ms, 直接后端的 20%) 内，输出一致、fixture 和外部状态字节保持不变。

真实原生隔离安装通过基础→增强→恢复基础、7.9.2→目标版、拒绝降级及中断→诊断→显式恢复；保留未知文件。基础缓存实际为十个 Skill、零本包 Hook，基础安装过程无 Python 前置。假宿主故障测试、原生安装和真实模型任务分别记录。

原生 Codex 0.154.0 的模型验收采用私有 app-server 内存令牌认证；未复制或持久化账户凭据。这不代表所有宿主或普通 codex exec 的认证路径已经验证。当前真实账户未升级、Desktop 未重启，不将隔离 CLI 结果投影为当前 Desktop 或业务项目已生效。

RELEASE_COMPLETE 与 INCIDENT_EFFECTIVE 分别判断：前者表示发行及发布后验证全部完成；后者还需具体事故现场的业务生效证据。本文件的包验证和隔离验收不能替代其他层级的实际读回。
