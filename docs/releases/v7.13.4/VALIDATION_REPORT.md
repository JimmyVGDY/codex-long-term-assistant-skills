# V7.13.4 验证记录

- 定向权限、名称规范化、完整名称匹配与迁移回归通过；独立原始失败样本保留。
- 最终行为基线的完整包测试 779 项、运行时测试 231 项通过；报告摘要见 `PACKAGE_VALIDATION.json`。后续范围文字调整须另行验证投影、链接及包完整性。
- 实际本地桌面组件为 `0.155.0-alpha.16.4`；安装、verify/doctor、普通沙箱载荷可读且不可写、新进程 Plugin 加载分别通过。
- 原生 V4 重入拦截通过。正向请求因不透明加密消息与已批准的明文摘要不一致而在创建前被拒绝，固定原因码为 `V4_REQUEST_MESSAGE_MISMATCH`；不宣称正向准入通过。
- 原生输入未提供解包元数据；官方参数改写机制在这次加密消息自发自收实验中导致解码错误。实验未创建子任务，临时 Hook 和信任项已恢复。
- 官方稳定组件的制品与源码摘要、内部多版本契约样本只证明其各自检查，不替代实际 Desktop 验收。
- CI、标签、发布资产、provenance、公开下载与站点读回仍是各自独立门禁；读回前不宣称完成。
