# V7.9.2 发行说明

修复 Plugin 安装后的默认能力注册表命令返回 `UNREADABLE`。注册表现在随受管 runtime 打包；源码继续使用权威 config，安装布局读取随包副本。显式 `--registry` 和失败关闭语义保持兼容。

新增七项隔离回归，包括真实打包器输出、25 项能力读回、源与副本一致性、缺失及损坏处理。写入门禁和预算授权不因本修复改变。

发布与账户安装结果以本标签的 GitHub Actions、Release 产物和后续现场读回为准。
