# V7.6.2 发行说明

本次补丁修复 Hook 的非阻断行为，并隔离旧版可选能力门禁状态。

- UserPromptSubmit 使用经兼容性证据确认的异步 Hook 注册；可选反馈不再阻断普通提示。
- 11 个冻结 Codex 版本分别绑定官方源码 tag、提交、路径与 SHA-256；Plugin 静态异步注册只在安装前宿主核验通过后落盘，未知版本失败关闭。
- 旧版门禁策略启用时，原生写入安全失败关闭；未配置或已停用策略保持中性。
- UserPromptSubmit 与 Stop 不读取或改写旧版 GateTask 状态。

本记录只覆盖已完成的实现与定向测试。完整发行验证、独立复审、提交、推送、公开发布、账户安装和实际生效读回不由本记录推断。
