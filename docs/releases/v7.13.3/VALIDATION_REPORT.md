# V7.13.3 验证记录

本文件是发布前源码记录；后续完整检查、制品、安装及公开发布以对应提交和动作读回为准。

| 项目 | 证据与边界 |
|---|---|
| Windows 路径回归 | 最终八文件修复的 18 项测试通过，覆盖别名查询、旧指纹、冲突、并发登记、显式账本、生命周期证明、权限异常、根保护及未创建子目标 |
| 既有 V4 路由 | 原子证明修正后的 111 项测试通过；随后仅调整未创建子目标判断并由最终 18 项覆盖 |
| 早期运行时检查 | 较早修订执行 231 项，228 通过、3 跳过；不替代最终版本完整验证 |
| 旧账本保护 | 源码只读查询使两种路径表示返回同一既有账本，核对前后账本字节一致；这不是原生回调证据 |
| 独立定向核验 | 独立桌面任务按八文件固定哈希核验，所提问题均修正；逻辑只读，不声明系统隔离或完整发行复审 |
| 发行验收 | 版本同步后的完整包、跨平台矩阵、可复现制品、安装与公开发布待分别读回 |
| 原生准入与模型资格 | 尚未通过；不启用 GPT-6 生产默认，不将源码或合成测试当作严格预算生效证据 |

历史策略、账本格式和评价门槛保持不变。无法获得文件身份时拒绝继续，既有失败、未知消耗及未通过的验收记录继续保留。
