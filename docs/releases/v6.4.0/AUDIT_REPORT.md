# Codex 跨项目长期技术助手 V6.4 审计报告

## 审计范围

审计覆盖 V6.4 相对 V6.3 的行为变化：Marketplace 所有权、Plugin cache 事务、state schema 迁移、payload 身份、事件分段、半记录恢复、宿主事实来源、能力探测、统一验证器及交付文档。

审计边界为逻辑只读 Reviewer。未执行提交、推送、部署、重启或生产操作。

## 实施前审查

实施前审查识别六组阻断根因：

1. Marketplace 不得以整树替换方式破坏未知资产；
2. Plugin cache 必须纳入 journal、备份、恢复和精确读回；
3. state schema 1 到 2 必须显式迁移并保留未知字段；
4. 生命周期、自观察与 CLI 必须共享跨段读取入口；
5. 跨段读取与轮换必须形成一致拓扑快照；
6. 活动尾部半记录与宿主事实来源必须采用失败关闭协议。

对应修订已进入设计和测试矩阵。

## 实施后第一轮审查

| 发现 | 等级 | 处理 |
|---|---|---|
| 非法终态静默降级为 UNKNOWN | 阻断 | 仅字段缺失可记 UNKNOWN；显式非法值抛出合同错误 |
| 已有事件只校验哈希、不校验 schema | 阻断 | 统一读取器增加严格字段、schema 和规范化校验 |
| actual_model 允许范围过宽 | 阻断 | 改为已知 Codex 模型精确集合 |
| 受管树后代 Junction/Reparse 未递归拒绝 | 中 | 摘要、备份、复制、删除前统一递归检查 |

## 实施后第二轮审查

第二轮使用新 packet 和新源码哈希定向复核四项修复。安全 Reviewer 与测试交付 Reviewer 均确认原发现关闭，无残留发现。复核时复用 65 项包级回归、6 项 Runtime 回归和定向安全测试证据。随后按 Codex 当前 Plugin 规范补齐 Marketplace policy/category/interface，并新增隔离真实 CLI 与升级兼容测试；真实生命周期又暴露了 Hook 模型字段缺失时的验收合同问题，修复后增加关联宿主会话证据测试，最终包级回归增至 67 项。

## 当前结论

源码级与候选包级阻断项已关闭。包级 67 项回归、Runtime 6 项回归及语义门禁通过。正式 ZIP、真实账户升级、新会话生命周期、三段 payload digest 和包外统一 attestation 尚未执行，因此当前审计结论为：候选实现通过，正式发行未验证。

只有后续真实宿主读回同时满足 Codex 0.150.1、Plugin 6.4.0 installed/enabled、10 Skill、7 Reviewer、6 Hook、事件链连续、项目双重隔离和 payload 身份一致，正式发行状态才能更新为通过。
