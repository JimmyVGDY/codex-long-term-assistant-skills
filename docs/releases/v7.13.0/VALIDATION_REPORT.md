# V7.13.0 验证记录

本记录覆盖兼容支持节点：冻结 V3 保持默认，新模型仅限明确评测。代码与制品验证和原生预算、模型资格分开判定；不声明默认切换完成。

| 边界 | 已有结果 |
|---|---|
| V4 路由、预算、审查、评测与观察 | 最终 Astra 并发修复后 111 项定向测试通过；使用合成宿主与质量夹具 |
| 桌面管理组件适配 | 4 项定向测试通过；另有本机只读组件/登记探针通过 |
| 安装器诊断回归 | 41 项 UX 测试通过 |
| 旧协议与证据复核 | 修复后 129 项相关检查通过，含冻结 Matrix Hook、Review Controller、策略与验收映射 |
| 双语与 Skill 入口 | 文档投影、双语覆盖及 16 个修改后中英文入口校验通过 |
| 本地完整包与运行时 | Astra 最后修复前：包 739 通过、1 跳过；运行时 231 通过。后续按受影响范围复验，不能相加为新的整包总数 |
| 受支持 Python CI 与可复现制品 | 行为与打包基线 a7c8f07 的四组 Windows/Ubuntu、Python 3.11/3.13 验证及双语构建共 7 个 CI 任务通过；本地标准 zlib 构建与 CI 字节一致。最终发布记录以其对应工作流和来源证明为准 |
| 新版桌面安装 | 候选安装与 verify 通过，源包、市场和缓存摘要一致；不等于原生门禁有效 |
| 原生 V4 派发 | 既有任务两次及独立新任务一次实际派发均缺少预占和创建回执；可见生命周期启动观察。配置已受信，但不能证明活动任务的派发前后路径有效；原生验收未通过 |
| 模型实测资格 | 36 案例、72 次配对实验已预登记；累计三次未通过原生门禁的调用保留真实消耗，资格样本为零；不把停止或无结果评为模型质量失败 |
| Commit / Push / Public Release / Effective | 行为与打包基线已提交、推送至 PR；公开发行另以标签、附件与来源证明读回。严格预算及 GPT-6 生产默认尚未生效 |

打包修复将暂存区的 Windows 启动脚本统一为 CRLF；20 项双语回归覆盖输入换行差异和源文件不变。Python 3.14 的 zlib-ng 与标准 zlib 可能产生不同压缩字节；包内文件内容已逐项核对一致，标准 zlib 的两次本地构建与 CI 完整 ZIP 一致。不同压缩库的 ZIP 哈希不能直接用作源码差异结论。

桌面自带管理组件查询只能证明配置与登记事实，不能代替当前任务原生验收。活动桌面组件和管理组件的文件摘要一致，未获得能定位派发前后回调缺失原因的日志；根因保留未知。修改后的非受管 Hook 必须经过正常信任流程；不修改可信摘要或启用 bypass 来冒充通过。不声明新模型生产资格、思考收益或真实计费。兼容支持版继续旧默认，后续默认切换仍需通过这些门槛。

基线证据：[a7c8f07 CI](https://github.com/JimmyVGDY/codex-long-term-assistant-skills/actions/runs/35942327785)、[候选 PR](https://github.com/JimmyVGDY/codex-long-term-assistant-skills/pull/14)。
