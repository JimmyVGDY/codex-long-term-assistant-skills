# V7.12.0 审计与交接

范围：可靠性候选、执行测试证据、治理路由回归、快速状态诊断、LIGHT 提示/去重复指引，以及对应的双语版本投影与候选发行材料。

- F01 的 50 项定向回归和 16 次压力重复均通过。F03 保留全历史验证，仅合并单次身份读取；规模测试覆盖 100、1,000、10,000 三档、每档 20 个样本，不将该结果表述为算法渐进复杂度改良。
- F02 已完成三场景评估，候选快捷路径暂不交付并恢复原 LIGHT 规则。simple 工具调用中位数为 6 到 5（-16.7%，未达到 20% 建议目标），时长未改善；resume 的基线为 5/5 通过而候选 4/5。该结果不归因于规则，也不从混合样本推导通用、稳定或因果性能收益。
- F04 的 50 项定向测试通过；旧 payload `948c` 的同账户、并行原生采样 20 对样本中，完整/快速中位数为 2924.6905 ms/110.248 ms，p95 为 3521.541 ms/115.587 ms，局部下降 96.23%。当前最终 payload 未重新测量；`--quick` 不提供完整验证或授权，结果不外推到其他环境。
- F05 的 52 个用例通过。原 16 个独立宿主任务为 13 通过、3 失败，失败记录保留；固定 profile 的 13 个未改变原始证据字节与 3 个定向补充任务组合，`HOST_FINAL_REPORT` 记录 16 个实际独立任务均通过、无缺失。该结果不是单一 cohort 通过率，也不是独立 router trace，只覆盖该 profile。F06 的完整本地执行后，19 组能力重投影和 9 项定向证据测试通过；报告不保存断言正文、回溯、命令输出或子测试参数。
- 安全复审第二轮通过；交付复审第二轮此前仅发现发行文档滞后，现已更正。Reviewer 均为逻辑只读，不能表述为系统隔离只读。
- 本地完整包验证已执行：617 项包测试为 616 通过、1 平台条件跳过，231 项 runtime 测试全通过。它针对未提交集成候选 `6641ed24626925b3445a88014621ed530f8c2ee8`，不替代最终提交、CI 或发布制品的证据。
- Codex CLI 兼容锚点推进到 `0.156.0`，维持当前版加前十个稳定版的规则。账户布局补齐同目录 `seal_worker.py`，缺失入口在创建进程前拒绝；安装后追加、封存、缺失/篡改检测与卸载回归通过。此交付路径修复采用 INLINE 定向核对，不据此声称新增独立复审。
- 当前安装 payload `4d16bf233697500fd2f4087f356037dbe61aa93af61670b289b44cd0b1a0abc7` 在源码、市场与缓存均为 245 文件且摘要一致，installed、enabled、HOST_COMPATIBLE 和备份均已读回。全新父子任务产生五类真实事件，5 条记录为 `SEALED_CURRENT`。
- 本机 elevated 沙箱在目录固定阶段返回重分析点错误。实机验收仅对验收进程采用官方 `unelevated` 回退与只读策略，全局配置未改变；不将此结果表述为 elevated 初始化已修复。最终提交 CI 和公开发行仍须单独读回。
- 双语 ZIP 两次候选构建可重复，但文档变更后必须重建，旧 ZIP 不可作为最终公开附件。提交、推送、标签、CI、制品、GitHub Release、账户安装、重启和新任务效果仍须逐项读回。

- 新增 `0.156.0` 宿主验收：13 个原始独立任务通过，一项宿主执行错误中止原批次；保留失败后补验该项及两项未启动场景。验证原始元数据、投影、报告和实际任务 ID 后，13＋3 的组合覆盖满足原 16 场景 profile，全部通过且无缺失。运行条件为临时 `unelevated`＋只读，仍是 `HOST_FINAL_REPORT`，不表示单批次全部成功或独立 router trace。
