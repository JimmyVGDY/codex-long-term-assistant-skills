<!-- Generated from locales/en/docs/releases/v7.11.1/VALIDATION_REPORT.md; edit that source and run scripts/documentation.py sync. -->

# V7.11.1 Validation

The installation and targeted checks below describe the local candidate snapshot from 2026-09-18. Pre-release revalidation is recorded at the end.

Scoring: 111 targeted tests passed in 119 executions, including 8 affected reruns. Independent review passed with no findings. Coverage includes 240 exhaustive oracle cases, 720 nonmerging-prefix checks and 6 frozen old scorecards.

Compatibility and version validation: 160 tests passed, including 19 bilingual artifact tests; 9 additional installer tests passed. Three DPAPI-dependent tests were rerun successfully as the local account in temporary directories. Builds used the repository owner account. These groups overlap and are not a single full-suite total.

Verified official Codex CLI 0.155.0 npm integrity, tagged source, CLI commands and Plugin output. Isolated Plugin installation and synthetic Hook protocol checks passed. The window contains 0.155.0 and ten preceding stable releases. Ubuntu and the complete native ten-profile dispatch matrix were not rerun.

On 2026-09-18, the actual account was upgraded from 7.11.0 to 7.11.1. Installation verify and strict doctor passed. At that point, all 245 payload files agreed across source, Marketplace and cache at digest `964216c77eabfe2725d3945dbcffc7bee59e738df53ab845bfb905827905fa8b`. The transaction rebuilt migrated account paths. The old host-bound keyring was preserved byte-for-byte; a new host keyring verified successfully. Old signatures and approvals remain unverified.

Projectless native read-only loading passed: the stable CLI read the 7.11.1 Skill from its catalog and executed a synthetic scoring check through the installed runtime, yielding 17 to 17 under the new formula and 9 under frozen replay. An initial attempt was blocked without Windows sandbox configuration; an invocation-only unelevated restricted sandbox then passed without persistent policy changes. No real Reviewer was dispatched; this arithmetic fixture grants no dispatch authorization.

Installation and acceptance used the separate stable CLI. The running Desktop's bundled 0.155.0-alpha.2.6 was not replaced, and this already-open task is not claimed to have reloaded. Record `RELEASE_COMPLETE=false` and `INCIDENT_EFFECTIVE=UNVERIFIED` separately. No commit, push, publication or remote workflow was performed.

## Pre-release revalidation on 2026-09-19

Complete package validation passed: 562 package tests and 231 runtime tests, with overall OK results; suite counts include platform-conditional skips. Reproducible builds and the worktree side-effect gate passed on a fixed source tree. Bilingual, semantic, policy projection, documentation consistency and link checks passed.

The first attempt exposed an inherited real CLI override in the test invocation, a local PowerShell execution-policy restriction, and stale release-attestation version fixtures and registry pins. The test invocation no longer overrides the fake CLI; RemoteSigned applies only to the test process. Current registry pins and fixtures were corrected without persistent execution-policy changes. Eleven bilingual comments/docstrings were completed without changing executable scoring logic.

The current source payload digest is `420954ba3dfcb6ed255b31fef3afce24ad27c5bd52f9cc0feb36ea5493afacf9`, recorded separately from the September 18 installation snapshot. Package validation does not establish remote compatibility, publication, Pages deployment or final account payload effectiveness; each requires separate readback.

Final account installation, verify and strict doctor passed. All 245 source and installed-cache files were verified; the account state records matching Marketplace and cache digests. This proves installation consistency, not reloading of the current open task or completion of the full native dispatch matrix.
