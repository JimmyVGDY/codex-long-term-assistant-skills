# V7.11.1 验证记录

以下安装与定向验证为 2026-09-18 本地候选快照；发布前复核见文末。

评分修复：111 个定向用例通过（119 次执行，含 8 次受影响重跑）；独立复审通过，无发现。包含 240 组独立穷举对照、720 个非合并前缀检查和 6 份旧公式快照。

兼容与版本验证：160 个用例通过，包括 19 个中英文制品测试；另有 9 个安装器定向用例通过。沙箱账户不支持 DPAPI 的 3 个用例已用本机账户在临时目录重跑通过；构建使用仓库所有者身份。以上分组存在重叠，不作为一次全量测试总数。

已核验官方 Codex CLI 0.155.0 的 npm 完整性、固定标签源码、CLI 命令与 Plugin 输出，隔离 Plugin 安装和合成 Hook 协议测试通过。兼容窗口包含 0.155.0 及此前十个稳定版；Ubuntu 与完整原生十档派发矩阵未重跑。

2026-09-18 账户 7.11.0 → 7.11.1 升级完成，安装 verify 和严格 doctor 均通过。当时源码、Marketplace、缓存共 245 个载荷文件一致，摘要为 `964216c77eabfe2725d3945dbcffc7bee59e738df53ab845bfb905827905fa8b`。迁移遗留的旧账户路径由安装事务重建；旧主机密钥环已原样备份，新主机密钥环验证通过，旧签名和旧审批保持不可验证。

仓库外原生只读加载通过：0.155.0 CLI 从技能目录读取 7.11.1 Skill，并执行已安装运行时的合成评分检查，结果为新算法 17 → 17、旧策略重放 9。首次调用因未配置 Windows 沙箱而阻断；仅本次指定官方支持的 unelevated 受限沙箱后通过，未改变持久安全配置。没有执行真实 Reviewer 派发，该算例不充当调度授权。

安装与验收使用独立的稳定版 CLI；正在运行的 Desktop 内置 0.155.0-alpha.2.6 未替换，也不声明当前已打开任务重新加载。`RELEASE_COMPLETE=false` 与 `INCIDENT_EFFECTIVE=UNVERIFIED` 分别记录；未提交、推送、发布或触发远端工作流。

## 2026-09-19 发布前复核

完整包验证通过：562 个包测试、231 个运行时测试，套件整体结果为 OK；测试数包含按平台条件跳过的项目。固定工作区上的可复现构建与工作区副作用门禁通过。双语、语义、策略投影、文档一致性和链接检查通过。

首轮验证发现测试进程误带入真实 CLI 路径、PowerShell 本机执行策略阻止脚本，以及发行证明旧版本样例和摘要遗漏。已移除测试调用中的 CLI 覆盖，只在测试进程使用 RemoteSigned，更新当前兼容登记表的固定摘要与样例；未修改持久执行策略。补齐 11 处双语注释和文档字符串，评分可执行逻辑未改变。

本轮源码载荷摘要为 `420954ba3dfcb6ed255b31fef3afce24ad27c5bd52f9cc0feb36ea5493afacf9`，与上述 9 月 18 日安装快照分别记录。包验证不等于远端兼容矩阵、公开发布、文档站部署或最终账户载荷生效；这些状态仍需独立读回。

本轮最终账户安装、verify 与严格 doctor 已通过，源码和已安装缓存的 245 个文件逐项校验通过，账户状态中的 Marketplace 与缓存摘要均匹配上述当前载荷。此读回证明安装一致性，不代表当前已打开任务重新加载或完整原生派发矩阵通过。
