"""Codex Cross-Project Runtime V5.0.

This package is the authoritative implementation for project binding,
approval/evidence contracts, memory promotion and finalization integrity.
It uses only the Python standard library and never performs deployment,
push, restart or production writes by itself.
"""

__version__ = "5.0.0"
