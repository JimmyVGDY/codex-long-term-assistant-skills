# 项目级 AGENTS.md 示例

> 按当前项目实际情况填写后放到仓库根目录。不要复制全局通用规则，只记录项目特有事实、命令、边界和覆盖值。

## 项目信息

- 项目名称：
- 仓库路径：
- 技术栈和版本：
- 构建命令：
- 后端定向测试命令：
- 前端正式构建命令：
- 本地启动命令：
- 部署方式：
- 正式技术文档目录：
- 文档命名和版本规范：

## 项目边界

- 允许修改：
- 禁止修改：
- 历史兼容要求：
- 数据库和中间件：
- 当前用户规模和性能目标：
- 核心业务成功边界：

## Git 与交付

- 默认分支：
- 提交格式：
- 功能边界和提交拆分规则：
- CHANGELOG 规范：
- 接口、数据库、部署和架构文档更新规则：
- 是否允许创建分支：
- 默认禁止推送、部署和重启，除非当前任务明确授权。

## 多 Agent 复审

- 默认风险级别：
- 必须启用的 Reviewer：
- 可豁免的 Reviewer：
- 最大并行 Reviewer（不得高于平台和全局上限）：
- 最大复审轮次：
- 最大集中修复轮次：
- 关键功能是否要求严格独立复审：
- 严格复审是否要求父会话整体只读：
- 可接受的最低隔离等级：system-readonly / logical-readonly
- Reviewer 运行时隔离证据保存位置：

## 长期任务外部记忆

- `<AGENT_CONTEXT_ROOT>`：
- 项目标识：
- 外部记忆必须位于仓库外：是
- 每个可恢复小节点更新 `CURRENT_TASK.md` 和 `PROGRESS.md`：是
- 连续实质性动作检查点上限：
- 最近恢复读取检查点数量：
- 共享记忆写入者：主协调 Agent

## 项目特有规则

-
