interface:
  display_name: "长期任务持续记忆"
  short_description: "小节点检查点、上下文恢复、多 Agent 单写者与交付记录"
  default_prompt: "使用长期任务持续记忆技能，把对话上下文视为短期缓存；每完成一个可恢复小节点就更新 CURRENT_TASK 和 PROGRESS，并在继续工作前核对检查点与实际 Git 状态。"
policy:
  allow_implicit_invocation: true
