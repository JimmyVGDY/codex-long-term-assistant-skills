interface:
  display_name: "Vue 前端工程"
  short_description: "Vue 组件、状态、路由、异步、实时连接与构建规则"
  default_prompt: "使用 Vue 前端工程技能处理当前任务，先识别 Vue 与构建版本，再检查组件状态、请求竞态、权限边界和资源释放。"
policy:
  allow_implicit_invocation: true
