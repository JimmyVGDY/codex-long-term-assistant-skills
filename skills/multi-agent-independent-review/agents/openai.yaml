interface:
  display_name: "多 Agent 独立复审"
  short_description: "Luna/Terra 分级、统一审查包与成本收敛复审"
  default_prompt: "仅在独立审查能增加有效信息时使用。先生成统一审查包，按 luna-low、luna-medium、terra-medium、terra-high 逐级路由，最高 Terra High；等待同轮结果后统一归因和定向复核。"
policy:
  allow_implicit_invocation: true
