interface:
  display_name: "多 Agent 独立复审"
  short_description: "Luna/Terra 分级、统一审查包与成本收敛复审"
  default_prompt: "仅在独立审查能增加有效信息时使用。生成统一审查包，从 Luna 的基础预算开始，按复审预算与有效证据加分，算完后选择十组合之一一次派发，最高 Astra High；等待同轮结果后统一归因和定向复核。"
policy:
  allow_implicit_invocation: true
