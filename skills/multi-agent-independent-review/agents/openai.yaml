interface:
  display_name: "多 Agent 独立复审"
  short_description: "并行六维审查、问题归并、集中修复与递归上限"
  default_prompt: "使用多 Agent 独立复审技能，对当前稳定差异进行风险分工并行审查，等待全部结果后统一归因，形成最小完整修复集合，并遵守深度、轮次和 Reviewer 总量上限。"
policy:
  allow_implicit_invocation: true
