# V7.9.0 Release Notes

V7.9.0 adds a Python-free native base installation and moves Hooks, runtime, account tools, state, budgets, controlled writes, global rules, and Reviewers into the optional enhancement transaction. Base and enhancement share one Skill source.

`doctor` and `status` now provide concise actionable summaries by default while retaining detailed JSON through `--json`.
